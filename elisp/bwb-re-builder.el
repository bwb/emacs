;;;; Configure `re-builder'.

(require 're-builder)

;; From http://www.masteringemacs.org/
;; "C-c C-w" yanks the current regex to the `kill-ring'.
(setq reb-re-syntax 'string)

(provide 'bwb-re-builder)
